import { CallCredentials, ChannelCredentials, ChannelOptions, ServerCredentials, experimental } from "@grpc/grpc-js";
import GrpcUri = experimental.GrpcUri;
import SecureConnector = experimental.SecureConnector;
export declare class XdsChannelCredentials extends ChannelCredentials {
    private fallbackCredentials;
    constructor(fallbackCredentials: ChannelCredentials);
    _isSecure(): boolean;
    _equals(other: ChannelCredentials): boolean;
    _createSecureConnector(channelTarget: GrpcUri, options: ChannelOptions, callCredentials?: CallCredentials): SecureConnector;
}
export declare class XdsServerCredentials extends ServerCredentials {
    private fallbackCredentials;
    constructor(fallbackCredentials: ServerCredentials);
    getFallbackCredentials(): ServerCredentials;
    _isSecure(): boolean;
    _equals(other: ServerCredentials): boolean;
}
